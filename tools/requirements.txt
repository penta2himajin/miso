# Python environment for tools/ (golden outputs, fixtures, inventories).
#   python3 -m venv .venv && .venv/bin/pip install -r tools/requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
transformers==5.17.0
gguf==0.19.0
numpy
